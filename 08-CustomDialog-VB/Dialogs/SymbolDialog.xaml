<Window
    x:Class="CustomDialog.Dialogs.SymbolDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:SpiceLogic.HtmlEditor.WPF.Controls;assembly=SpiceLogic.HtmlEditor.WPF"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ignore="http://www.ignore.com"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="1"
    Height="1"
    MinWidth="950"
    MinHeight="385"
    AllowsTransparency="True"
    Background="{x:Null}"
    ResizeMode="CanResizeWithGrip"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None"
    d:DesignHeight="400"
    d:DesignWidth="747"
    mc:Ignorable="d ignore"
    Closed="SymbolDialog_Closed">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SpiceLogic.HtmlEditor.WPF;component/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <controls:DialogHeader Title="Custom Dialog : Symbols" Grid.Row="0" Panel.ZIndex="1" MouseLeftButtonDown="DialogHeader_MouseLeftButtonDown" />
        <Grid Grid.Row="1" Margin="0,-10,0,0" Background="{DynamicResource DialogBackgroundBrush}" />
        <Grid Grid.Row="1">
            <ScrollViewer VerticalScrollBarVisibility="Hidden">
                <WrapPanel x:Name="SymbolPanel" Margin="5" HorizontalAlignment="Center">
                </WrapPanel>
            </ScrollViewer>
        </Grid>
        <StackPanel Grid.Row="2" Panel.ZIndex="1" Margin="0,-9,0,0">
            <Border Grid.Row="1" Width="Auto" Height="8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{DynamicResource DialogHeaderShadowBrush}" RenderTransformOrigin="0.5,0.5" >
                <Border.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleY="-1" ScaleX="1"/>
                        <SkewTransform AngleY="0" AngleX="0"/>
                        <RotateTransform Angle="0"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Border.RenderTransform>
            </Border>
            <Rectangle Height="16" Margin="0" Grid.Row="1" VerticalAlignment="Bottom" Fill="#FFB0BCBC"/>
        </StackPanel>
        <Button HorizontalAlignment="Right" VerticalAlignment="Top" Width="20" Height="20" Panel.ZIndex="2" Margin="0,6,7,0" Style="{DynamicResource DialogCloseButtonStyle}" Click="CloseButtonClicked" />
    </Grid>
</Window>