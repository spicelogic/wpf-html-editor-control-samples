<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:colorPickerDialog="clr-namespace:CustomDialog.Dialogs.ColorPickerDialog"
                    mc:Ignorable="d">

    <DrawingBrush x:Key="CheckerBrush" TileMode="Tile" Viewport="0,0,10,10" ViewportUnits="Absolute">
        <DrawingBrush.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="White">
                    <GeometryDrawing.Geometry>
                        <RectangleGeometry Rect="0,0 100,100" />
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
                <GeometryDrawing Brush="LightGray">
                    <GeometryDrawing.Geometry>
                        <GeometryGroup>
                            <RectangleGeometry Rect="0,0 50,50" />
                            <RectangleGeometry Rect="50,50 50,50" />
                        </GeometryGroup>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingBrush.Drawing>
    </DrawingBrush>

    <!--  Spectrum Slider Styles  -->
    <Style x:Key="SliderRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="{TemplateBinding Background}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{x:Type colorPickerDialog:ColorThumb}" TargetType="{x:Type colorPickerDialog:ColorThumb}">

        <Setter Property="PointerOutlineBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}" />
        <Setter Property="PointerOutlineThickness" Value="3" />
        <Setter Property="Foreground" Value="{x:Null}" />
        <Setter Property="ThumbColor" Value="Transparent" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type colorPickerDialog:ColorThumb}">
                    <Border Background="{TemplateBinding Background}" BorderThickness="1" BorderBrush="#FFACACAC">
                        <Grid>
                            <Rectangle Name="arrow" Stretch="Fill" Fill="{Binding ThumbColor, RelativeSource={RelativeSource TemplatedParent}}" Stroke="#FFF0F0F0" StrokeThickness="3" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="VerticalSliderLeftThumbStyle" TargetType="{x:Type Thumb}">
        <Setter Property="Foreground" Value="{x:Null}" />
        <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
        <Setter Property="Width" Value="21" />
        <Setter Property="Height" Value="11" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Canvas>
                        <Path Data="M 21 11 L 21 0 L 6 0 L 0 5 L 0 6 L 6 11 z" Fill="{TemplateBinding Background}" />
                        <!--  TopTip/Top OuterLine  -->
                        <Path Data="M 20 0.5 L 5.5 0.5 L 0.5 5 L 0.5 5.5 " Stroke="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}" StrokeThickness="1.0" />
                        <!--  Right/Bottom/BottomTip OuterLine  -->
                        <Path Data="M 0.5 5.5 L 0.5 6 L 5.5 10.5 L 20.5 10.5 L 20.5 0 " Stroke="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}" StrokeThickness="1.0" />
                        <!--  TopTip/Top InnerLine  -->
                        <Path Data="M 19 1.5 L 5.5 1.5 L 1.5 5 L 1.5 5.5 " Stroke="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" StrokeThickness="1.0" />
                        <!--  Right/Bottom/BottomTip InnerLine  -->
                        <Path Data="M 1.5 5.5 L 1.5 6 L 5.5 9.5 L 19.5 9.5 L 19.5 1.0 " Stroke="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" StrokeThickness="1.0" />
                        <!--  Fill Inner Area  -->
                        <Path Data="M 19 9 L 19 2 L 6 2 L 2 5.5 L 6 9 z" Fill="{TemplateBinding Foreground}" Opacity="0.5" />
                        <!--  Fill Background Area including base border  -->
                    </Canvas>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsDragging" Value="true">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="VerticalSliderRightThumbStyle" TargetType="{x:Type Thumb}">
        <Setter Property="BorderThickness" Value="0,0,0,0" />
        <Setter Property="Foreground" Value="{x:Null}" />
        <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
        <Setter Property="Width" Value="21" />
        <Setter Property="Height" Value="11" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Canvas>

                        <Path Data="M 0 11 L 0 0 L 15 0 L 21 5 L 21 6 L 15 11 z" Fill="{TemplateBinding Background}" />

                        <!--  Left/Top/TopTip OuterLine  -->
                        <Path Data="M 0.5 10 L 0.5 0.5 L 15.5 0.5 L 20.5 5 L 20.5 5.5 " Stroke="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}" StrokeThickness="1.0" />

                        <!--  Bottom/BottomTip OuterLine  -->
                        <Path Data="M 20.5 5.5 L 20.5 6 L 15.5 10.5 L 0 10.5 " Stroke="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}" StrokeThickness="1.0" />

                        <!--  Left/Top/TopTip InnerLine  -->
                        <Path Data="M 1.5 9 L 1.5 1.5 L 15.5 1.5 L 19.5 5 L 19.5 5.5 " Stroke="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" StrokeThickness="1.0" />

                        <!--  Bottom/BottomTip InnerLine  -->
                        <Path Data="M 19.5 5.5 L 19.5 6 L 15.5 9.5 L 1 9.5 " Stroke="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" StrokeThickness="1.0" />

                        <!--  Fill Inner Area  -->
                        <Path Data="M 2 9 L 2 2 L 15 2 L 19 5.5 L 15 9 " Fill="{TemplateBinding Foreground}" Opacity="0.5" />
                        <!--  Fill Background Area including base border  -->
                    </Canvas>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{x:Type colorPickerDialog:SpectrumSlider}" TargetType="{x:Type colorPickerDialog:SpectrumSlider}">
        <Setter Property="Orientation" Value="Vertical" />
        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
        <Setter Property="Minimum" Value="0" />
        <Setter Property="Maximum" Value="100" />
        <Setter Property="TickFrequency" Value="0.001" />
        <Setter Property="IsSnapToTickEnabled" Value="True" />
        <Setter Property="IsDirectionReversed" Value="False" />
        <Setter Property="Value" Value="1" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type colorPickerDialog:SpectrumSlider}">
                    <Border Height="{TemplateBinding Height}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" MinWidth="{TemplateBinding Slider.MinWidth}" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border x:Name="PART_TrackBackground" Grid.Column="1" Width="18" Margin="10,0,10,0">
                                <Rectangle x:Name="PART_SpectrumDisplay" VerticalAlignment="Stretch" Stretch="Fill" />
                                <Border.BitmapEffect>
                                    <BevelBitmapEffect BevelWidth="3" EdgeProfile="BulgedUp" Relief="0.3" />
                                </Border.BitmapEffect>
                            </Border>

                            <Canvas Grid.Column="1" Width="4" HorizontalAlignment="Center">
                                <Rectangle x:Name="PART_SelectionRange" Canvas.Left="1" Width="2.0" Fill="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" Visibility="Hidden" />
                            </Canvas>

                            <Track Name="PART_Track" Grid.Column="1">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge" Style="{StaticResource SliderRepeatButtonStyle}" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge" Style="{StaticResource SliderRepeatButtonStyle}" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <colorPickerDialog:ColorThumb x:Name="Thumb" Height="11" ThumbColor="{Binding SelectedColor, RelativeSource={RelativeSource TemplatedParent}}" Width="28" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!--  Display SelectionRange background  -->
                        <Trigger Property="IsSelectionRangeEnabled" Value="True">
                            <Setter TargetName="PART_SelectionRange" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="Width" Value="Auto">
                            <Setter Property="MinWidth" Value="21" />
                        </Trigger>
                        <Trigger Property="Height" Value="Auto">
                            <Setter Property="MinHeight" Value="104" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!--  End Spectrum Slider  -->

    <!--  ColorPicker  Styles  -->
    <Style x:Key="{x:Type colorPickerDialog:ColorPicker}" TargetType="{x:Type colorPickerDialog:ColorPicker}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type colorPickerDialog:ColorPicker}">

                    <Border Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" d:DesignWidth="634.46" d:DesignHeight="425.787">

                        <Grid Margin="{TemplateBinding Padding}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource CheckerBrush}" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}" BorderThickness="2">

                                <Border Name="PART_ColorDetail" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Cursor="Cross">
                                    <Border.Background>
                                        <DrawingBrush Opacity="{Binding ElementName=opacitySlider, Path=Value}">
                                            <DrawingBrush.Drawing>
                                                <DrawingGroup>
                                                    <GeometryDrawing>
                                                        <GeometryDrawing.Geometry>
                                                            <RectangleGeometry Rect="0,0,100,100" />
                                                        </GeometryDrawing.Geometry>
                                                        <GeometryDrawing.Brush>
                                                            <SolidColorBrush Color="{Binding ElementName=PART_ColorSlider, Path=SelectedColor}" />
                                                        </GeometryDrawing.Brush>
                                                    </GeometryDrawing>
                                                    <GeometryDrawing>
                                                        <GeometryDrawing.Geometry>
                                                            <RectangleGeometry Rect="0,0,100,100" />
                                                        </GeometryDrawing.Geometry>
                                                        <GeometryDrawing.Brush>
                                                            <LinearGradientBrush StartPoint="0.5,1" EndPoint="0.5,0">
                                                                <GradientStop Offset="0.0" Color="Black" />
                                                                <GradientStop Offset="1.0" Color="{Binding ElementName=PART_ColorSlider, Path=SelectedColor}" />
                                                            </LinearGradientBrush>
                                                        </GeometryDrawing.Brush>
                                                    </GeometryDrawing>
                                                    <DrawingGroup>
                                                        <GeometryDrawing>
                                                            <GeometryDrawing.Geometry>
                                                                <RectangleGeometry Rect="0,0,100,100" />
                                                            </GeometryDrawing.Geometry>
                                                            <GeometryDrawing.Brush>
                                                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                                    <GradientStop Offset="0.0" Color="White" />
                                                                    <GradientStop Offset="1" Color="Black" />
                                                                </LinearGradientBrush>
                                                            </GeometryDrawing.Brush>
                                                        </GeometryDrawing>
                                                        <DrawingGroup.OpacityMask>
                                                            <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                                <GradientStop Offset="0.0" Color="#FF000000" />
                                                                <GradientStop Offset="1" Color="#00000000" />
                                                            </LinearGradientBrush>
                                                        </DrawingGroup.OpacityMask>
                                                    </DrawingGroup>
                                                </DrawingGroup>
                                            </DrawingBrush.Drawing>
                                        </DrawingBrush>
                                    </Border.Background>
                                    <Path Name="PART_ColorMarker" Cursor="Cross" Fill="Transparent" StrokeThickness="2">
                                        <Path.Data>
                                            <EllipseGeometry RadiusX="7" RadiusY="7" />
                                        </Path.Data>
                                        <Path.Stroke>
                                            <SolidColorBrush x:Name="markerBrush" Color="White" />
                                        </Path.Stroke>
                                    </Path>
                                </Border>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="0" Orientation="Horizontal" Grid.Row="0" Grid.RowSpan="2">
                                <colorPickerDialog:SpectrumSlider x:Name="PART_ColorSlider" Margin="0" IsMoveToPointEnabled="True" IsSnapToTickEnabled="False" Maximum="360" Value="0" Height="Auto" />
                                <Grid Width="18">
                                    <Border VerticalAlignment="Stretch" Background="{StaticResource CheckerBrush}" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}" Margin="0" Width="Auto" />
                                    <Border VerticalAlignment="Stretch" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}" Margin="0" Width="Auto">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                <LinearGradientBrush.RelativeTransform>
                                                    <TransformGroup>
                                                        <ScaleTransform CenterY="0.5" CenterX="0.5" />
                                                        <SkewTransform CenterY="0.5" CenterX="0.5" />
                                                        <RotateTransform Angle="-90" CenterY="0.5" CenterX="0.5" />
                                                        <TranslateTransform />
                                                    </TransformGroup>
                                                </LinearGradientBrush.RelativeTransform>
                                                <GradientStop Offset="0.0" Color="White" />
                                                <GradientStop Offset="1.0" Color="Transparent" />
                                            </LinearGradientBrush>
                                        </Border.Background>
                                    </Border>

                                    <Slider x:Name="opacitySlider" IsDirectionReversed="True" LargeChange="0.02" Maximum="1" Minimum="0" Orientation="Vertical" SmallChange="0.01" TickFrequency="0.01" Value="{Binding ScA, RelativeSource={RelativeSource TemplatedParent}}" RenderTransformOrigin="0.5,0.5">
                                        <Slider.RenderTransform>
                                            <TransformGroup>
                                                <ScaleTransform ScaleX="1.35" />
                                                <SkewTransform />
                                                <RotateTransform />
                                                <TranslateTransform />
                                            </TransformGroup>
                                        </Slider.RenderTransform>
                                    </Slider>
                                </Grid>
                            </StackPanel>

                            <Grid Grid.Row="1" Grid.Column="2" Margin="20,0,0,0" HorizontalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0" Grid.ColumnSpan="3">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Style="{DynamicResource ThinTextBlockStyle}" Margin="0,0,0,5">
                                        Selected Color
                                    </TextBlock>
                                    <Border Grid.Row="1" Margin="0,0,0,15">
                                        <Rectangle MinWidth="50" MinHeight="40" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" RadiusX="0" RadiusY="0" Stretch="Fill" Margin="0,5,0,0">
                                            <Rectangle.Fill>
                                                <SolidColorBrush Color="{Binding SelectedColor, RelativeSource={RelativeSource TemplatedParent}}" />
                                            </Rectangle.Fill>
                                        </Rectangle>
                                    </Border>
                                    <Border BorderBrush="#FFB2B2B2" BorderThickness="1" HorizontalAlignment="Stretch" Height="1"
                                            VerticalAlignment="Bottom" Width="Auto" Grid.Row="1" />
                                </Grid>

                                <Grid Grid.Row="1" Grid.Column="0" Margin="2,10,20,2">

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                        <RowDefinition />
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Style="{DynamicResource ThinTextBlockStyle}" Margin="0,1,0,10">
                                        <Bold>ScRGB</Bold>
                                    </TextBlock>
                                    <TextBlock Grid.Row="1" Grid.Column="0" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" Text="ScA" VerticalAlignment="Center" Style="{DynamicResource ThinTextBlockStyle}" Margin="0,1,5,0" />
                                    <TextBox Grid.Row="1" Grid.Column="1" MinWidth="75" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" MaxLength="10" Text="{Binding ScA, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Width="80" Margin="0,1,0,0" />
                                    <TextBlock Grid.Row="2" Grid.Column="0" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" Text="ScR" VerticalAlignment="Center"  Style="{DynamicResource ThinTextBlockStyle}" Margin="0,1,5,0" />
                                    <TextBox Grid.Row="2" Grid.Column="1" MinWidth="75" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" MaxLength="10" Text="{Binding ScR, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Width="80" Margin="0,1,0,0" />
                                    <TextBlock Grid.Row="3" Grid.Column="0" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" Text="ScG" VerticalAlignment="Center" Style="{DynamicResource ThinTextBlockStyle}" Margin="0,1,5,0" />
                                    <TextBox Grid.Row="3" Grid.Column="1" MinWidth="75" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" MaxLength="10" Text="{Binding ScG, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Width="80" Margin="0,1,0,0" />
                                    <TextBlock Grid.Row="4" Grid.Column="0" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" Text="ScB" VerticalAlignment="Center" Style="{DynamicResource ThinTextBlockStyle}" Margin="0,1,5,0" />
                                    <TextBox Grid.Row="4" Grid.Column="1" MinWidth="75" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" MaxLength="10" Text="{Binding ScB, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Width="80" Margin="0,1,0,0" />
                                </Grid>

                                <Grid Grid.Row="1" Grid.Column="2" Margin="2,10,2,2">

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                        <RowDefinition />
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Style="{DynamicResource ThinTextBlockStyle}" Margin="0,1,0,10" HorizontalAlignment="Right">
                                        <Bold>sRGB</Bold>
                                    </TextBlock>
                                    <TextBlock Grid.Row="1" Grid.Column="0" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" Text="A" Margin="0,1,5,0" VerticalAlignment="Center" />
                                    <TextBox Grid.Row="1" Grid.Column="1" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" MaxLength="3" Text="{Binding A, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Margin="0,1,0,0" />
                                    <TextBlock Grid.Row="2" Grid.Column="0" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" Text="R" Margin="0,1,5,0" VerticalAlignment="Center" />
                                    <TextBox Grid.Row="2" Grid.Column="1" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" MaxLength="3" Text="{Binding R, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Margin="0,1,0,0" />
                                    <TextBlock Grid.Row="3" Grid.Column="0" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" Text="G" Margin="0,1,5,0" VerticalAlignment="Center" />
                                    <TextBox Grid.Row="3" Grid.Column="1" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" MaxLength="3" Text="{Binding G, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Margin="0,1,0,0" />
                                    <TextBlock Grid.Row="4" Grid.Column="0" Style="{DynamicResource ThinTextBlockStyle}" Text="B" Margin="0,1,5,0" VerticalAlignment="Center" />
                                    <TextBox Grid.Row="4" Grid.Column="1" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" MaxLength="3" Text="{Binding B, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Margin="0,1,0,0" />
                                </Grid>

                                <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="2,10,0,0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Style="{DynamicResource ThinTextBlockStyle}" Margin="0,10,0,0">
                                        Hexadecimal Notation
                                    </TextBlock>
                                    <TextBox Grid.Row="1" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" MaxLength="9" Text="{Binding HexadecimalString, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Margin="0,5,0,0" />
                                    <Border BorderBrush="#FFB2B2B2" BorderThickness="1" HorizontalAlignment="Stretch" Height="1" VerticalAlignment="Top" Width="Auto" Grid.Row="0" />
                                </Grid>
                            </Grid>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>