<Window x:Class="MvvmDataBinding.ElementBinding"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:se="clr-namespace:SpiceLogic.HtmlEditor.WPF;assembly=SpiceLogic.HtmlEditor.WPF"
        Title="Element binding" Height="450" Width="860">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Description -->
        <Border Grid.Row="0" Background="#F0F6FF" BorderBrush="#B0C4DE" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,10">
            <TextBlock TextWrapping="Wrap" Foreground="#333" LineHeight="22">
                <Bold>Element binding:</Bold> no view model here. The text box binds directly to the editor's
                <Bold>BodyHtml</Bold> using an ElementName binding, which shows that the editor works as a binding
                source for other controls just as well as it works as a binding target.
            </TextBlock>
        </Border>

        <Label Grid.Row="1">Editor.BodyHtml as a binding source</Label>
        <se:WpfHtmlEditor x:Name="Editor" Grid.Row="2"/>
        <Label Grid.Row="3">Text box bound to Editor.BodyHtml</Label>
        <TextBox Grid.Row="4" Text="{Binding ElementName=Editor, Path=BodyHtml}" TextWrapping="Wrap"/>
    </Grid>
</Window>
